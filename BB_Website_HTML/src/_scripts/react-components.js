//  -------------------------------------------
//  All Components
//  -------------------------------------------

const Box = styled.div(({ sx }) => ({ ...sx }));

const ProductSlider = ({ classes }) => {
  return <div className={`mb-40 ${classes}`}>Product slider here</div>;
};
