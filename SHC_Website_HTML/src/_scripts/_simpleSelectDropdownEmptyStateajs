//  -------------------------------------------
//  Empty Select
//  -------------------------------------------
$("select").on("change", function () {
  if ($(this).val()) {
    $(this).removeClass("not-selected");
  } else {
    $(this).addClass("not-selected");
  }
});